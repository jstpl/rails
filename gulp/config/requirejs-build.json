{
    "appDir": "../../rjs",
    "mainConfigFile": "../../rjs/src/index.js",
    "dir": "../../dist-rjs",
    // List the modules that will be optimized.
    "modules": [
        {
            // module names are relative to baseUrl
            "name": "index"
        }
    ]
}
